indexer
{
}

searchd
{
  listen = 127.0.0.1:9306:mysql41
  log = /home/rayko/desarrollo/altoros/rss_reader/log/development.searchd.log
  query_log = /home/rayko/desarrollo/altoros/rss_reader/log/development.searchd.query.log
  pid_file = /home/rayko/desarrollo/altoros/rss_reader/log/development.sphinx.pid
  workers = threads
  binlog_path = /home/rayko/desarrollo/altoros/rss_reader/tmp/binlog/development
}

source article_core_0
{
  type = pgsql
  sql_host = localhost
  sql_user = rssreader
  sql_pass = rssreader
  sql_db = rssreader_development
  sql_query = SELECT "articles"."id" * 1 + 0 AS "id", 'Article' AS "sphinx_internal_class_name", "articles"."title" AS "title", "articles"."description" AS "description", "articles"."id" AS "sphinx_internal_id", 'Article' AS "sphinx_internal_class", 0 AS "sphinx_deleted", "articles"."channel_id" AS "channel_id", extract(epoch from "articles"."created_at")::int AS "created_at", extract(epoch from "articles"."updated_at")::int AS "updated_at", extract(epoch from "articles"."pub_date")::int AS "pub_date" FROM "articles"  WHERE ("articles"."id" BETWEEN $start AND $end) GROUP BY "articles"."id", "articles"."title", "articles"."description", "articles"."id", "articles"."channel_id", "articles"."created_at", "articles"."updated_at", "articles"."pub_date"
  sql_query_range = SELECT COALESCE(MIN("articles"."id"), 1), COALESCE(MAX("articles"."id"), 1) FROM "articles" 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = channel_id
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_timestamp = pub_date
  sql_attr_string = sphinx_internal_class
  sql_field_string = title
  sql_query_info = SELECT "articles".* FROM "articles"  WHERE ("articles"."id" = ($id - 0) / 1)
}

index article_core
{
  type = plain
  path = /home/rayko/desarrollo/altoros/rss_reader/db/sphinx/development/article_core
  docinfo = extern
  charset_type = utf-8
  source = article_core_0
}
