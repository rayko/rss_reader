%h1 Listing channels

%table
  %tr
    %th Title
    %th Url
    %th
    %th
    %th

  - @channels.each do |channel|
    %tr
      %td= channel.title
      %td= channel.url
      %td= link_to 'Show', user_channel_path(channel)
      %td= link_to 'Edit', edit_user_channel_path(channel)
      %td= link_to 'Destroy', user_channel_path(channel), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Add Channel', new_user_channel_path
