%div.text-center
  .btn-group
    - if controller_name != 'sessions'
      = link_to "Sign In", new_session_path(resource_name), :class => 'btn btn-small'
    - if devise_mapping.registerable? && controller_name != 'registrations'
      = link_to "Sign Up", new_registration_path(resource_name), :class => 'btn btn-small'
    - if devise_mapping.recoverable? && controller_name != 'passwords'
      = link_to "Recover Pass", new_password_path(resource_name), :class => 'btn btn-small'
    - if devise_mapping.confirmable? && controller_name != 'confirmations'
      = link_to "Request Confirmation Info", new_confirmation_path(resource_name), :class => 'btn btn-small'
    - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
      = link_to "Request Unlock", new_unlock_path(resource_name), :class => 'btn btn-small'
